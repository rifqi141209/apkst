@extends('layouts.app')
@section('title', 'Dashboard Admin - KASTORE')
@section('content')
<section class="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-xs font-bold uppercase tracking-widest text-electric">Panel admin</p><h1 class="mt-2 font-display text-3xl font-bold">Kelola produk & stok</h1><p class="mt-2 text-sm text-slate-500">Perubahan stok langsung terlihat di katalog pengguna.</p></div><form method="POST" action="{{ route('logout') }}">@csrf<button class="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold text-slate-600" type="submit">Keluar</button></form></div>@if(session('success'))<div class="mt-6 rounded-xl bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700">{{ session('success') }}</div>@endif @if($errors->any())<div class="mt-6 rounded-xl bg-rose-50 px-4 py-3 text-sm font-semibold text-rose-700">{{ $errors->first() }}</div>@endif
<div class="mt-8 grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><form method="POST" action="{{ route('admin.products.store') }}" class="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-soft">@csrf<h2 class="font-display text-lg font-bold">Tambah produk</h2><div class="mt-4 space-y-3"><input name="name" required placeholder="Nama produk" class="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><select name="category" class="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><option>PRODUCTIVITY</option><option>STREAMING</option><option>MUSIC</option><option>EDUCATION</option></select><input name="price" type="number" min="100" required placeholder="Harga (Rp)" class="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><input name="stock" type="number" min="0" required placeholder="Stok awal" class="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><input name="image_color" pattern="[0-9A-Fa-f]{6}" placeholder="Warna gambar, contoh 2563EB" class="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><button class="w-full rounded-xl bg-electric py-3 text-sm font-bold text-white" type="submit">Tambah ke katalog</button></div></form>
<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-soft"><div class="border-b border-slate-100 p-5"><h2 class="font-display text-lg font-bold">Daftar produk</h2><p class="mt-1 text-xs text-slate-500">Stok bertambah setelah akun baru disimpan.</p></div><div class="divide-y divide-slate-100">@forelse($products as $product)<div class="p-5"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 class="font-bold">{{ $product->name }}</h3><p class="mt-1 text-xs text-slate-500">{{ $product->category }} · Rp {{ number_format($product->price, 0, ',', '.') }} · <strong class="text-ink">{{ $product->stock }} akun tersedia</strong></p></div><div class="flex flex-col gap-2 sm:flex-row"><form method="POST" action="{{ route('admin.products.accounts.store', $product) }}" class="flex flex-col gap-2 sm:flex-row">@csrf<input name="login" type="text" required placeholder="Email / username akun" class="rounded-lg border border-slate-200 px-3 py-2 text-sm"><input name="password" type="text" required placeholder="Password akun" class="rounded-lg border border-slate-200 px-3 py-2 text-sm"><button class="rounded-lg bg-ink px-3 py-2 text-xs font-bold text-white" type="submit">Tambah akun stok</button></form><form method="POST" action="{{ route('admin.products.destroy', $product) }}" onsubmit="return confirm('Arsipkan produk ini dari katalog? Riwayat invoice tetap aman.');">@csrf @method('DELETE')<button class="w-full rounded-lg border border-rose-200 px-3 py-2 text-xs font-bold text-rose-600 hover:bg-rose-50 sm:w-auto" type="submit"><i class="fa-solid fa-box-archive mr-1"></i>Hapus produk</button></form></div></div></div>@empty<p class="p-5 text-sm text-slate-500">Belum ada produk. Tambahkan produk pertama.</p>@endforelse</div></div></div></section>
@endsection
