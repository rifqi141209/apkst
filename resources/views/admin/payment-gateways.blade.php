@extends('layouts.app')
@section('title', 'Payment Gateway - Kastore')
@section('content')
<section class="mx-auto max-w-5xl px-4 py-10 sm:px-6 lg:px-8"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route('admin.dashboard') }}" class="text-sm font-semibold text-electric"><i class="fa-solid fa-arrow-left mr-1"></i> Kembali ke dashboard</a><p class="mt-6 text-xs font-bold uppercase tracking-widest text-electric">Pengaturan pembayaran</p><h1 class="mt-2 font-display text-3xl font-bold">Payment gateway</h1><p class="mt-2 text-sm text-slate-500">Pilih metode pembayaran yang tersedia saat checkout pengguna.</p></div><div class="rounded-xl bg-blue-50 px-4 py-3 text-xs font-semibold text-blue-700"><i class="fa-solid fa-circle-info mr-1"></i> Mode konfigurasi lokal</div></div>@if(session('success'))<div class="mt-6 rounded-xl bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700">{{ session('success') }}</div>@endif
<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($gateways as $gateway)<article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-soft"><div class="flex items-start justify-between gap-4"><div class="grid h-11 w-11 place-items-center rounded-xl {{ $gateway->active ? 'bg-emerald-100 text-emerald-600' : 'bg-slate-100 text-slate-400' }}"><i class="fa-solid fa-credit-card text-lg"></i></div><span class="rounded-full px-2.5 py-1 text-[11px] font-bold {{ $gateway->active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $gateway->active ? 'Aktif' : 'Nonaktif' }}</span></div><h2 class="mt-5 font-display text-lg font-bold">{{ $gateway->name }}</h2><p class="mt-1 min-h-10 text-sm leading-5 text-slate-500">{{ $gateway->description }}</p><form method="POST" action="{{ route('admin.payment-gateways.toggle', $gateway) }}" class="mt-5">@csrf @method('PATCH')<button type="submit" class="w-full rounded-xl py-2.5 text-sm font-bold {{ $gateway->active ? 'border border-slate-200 text-slate-600 hover:bg-slate-50' : 'bg-electric text-white hover:bg-blue-700' }}">{{ $gateway->active ? 'Nonaktifkan' : 'Aktifkan gateway' }}</button></form></article>@empty<div class="rounded-xl bg-white p-6 text-sm text-slate-500">Belum ada payment gateway.</div>@endforelse</div></section>
@endsection
